import 'package:flutter/material.dart';

class HotPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Hot Page')),
      body: Center(child: Text('Hot Page Content')),
    );
  }
}
